
# Audiobook Creator

Convert multiple MP3 files into a single M4B audiobook with chapters and cover image using Docker and FFmpeg.

## Usage

1. Place your MP3 files in the `input` folder.
2. Run:
	```
	docker compose up
	```
3. The output audiobook will be in the `output` folder as `audiobook.m4b`.
